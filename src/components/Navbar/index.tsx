import "./style.css";

const NavBar = () => {
  return (
    <header>
      <div className="navbar">
        <button className="connect-metamask">
          <p>Connect Metamask</p>
        </button>
      </div>
    </header>
  );
};

export default NavBar;
